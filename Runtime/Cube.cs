using UnityEngine;

public class Cube : MonoBehaviour
{
    public ScriptableObject cubeData;
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
